<div class="for-product">
  {{#each products}}
      <div class="product-container">
        <img src="{{image}}" alt="{{name}}">
        <h2>{{name}}</h2>
        <p>{{description}}</p>
        <p>${{price}}</p>

      </div>
    {{/each}}
</div>